# Render Blueprint: для микросервисного стека удобнее VPS + docker compose.
# См. docker-compose.prod.yml и README.md.
#
# Ниже — заглушка; полный деплой 6 сервисов + Postgres на free tier не поддерживается одним web-сервисом.

version: "1"

services: []
